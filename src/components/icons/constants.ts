import { theme } from '@ieum/styles';

export const DEFAULT_ICON_SIZE: number = 16;
export const DEFAULT_ICON_FILL: string = theme.colors.text;
export const DEFAULT_ICON_STROKE: string = theme.colors.text;
